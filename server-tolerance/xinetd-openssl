service openssl
{
  type = UNLISTED
  disable = no
  socket_type = stream
  protocol = tcp
  wait = no
  user = root
  bind = 0.0.0.0
  port = 9999
  server = /bin/bash
  server_args = /tester/run-openssl
}
